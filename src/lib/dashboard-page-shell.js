/** Full-width dashboard/admin page shell. Horizontal padding is on the app layout (`p-[10px]`). */
export const DASHBOARD_PAGE_SHELL =
  "flex h-full min-h-0 w-full min-w-0 flex-1 flex-col overflow-hidden";

/** Scrollable dashboard page without table fill-height. */
export const DASHBOARD_PAGE_BODY = "flex min-h-0 w-full min-w-0 flex-1 flex-col";
