export const metadata = {
  title: "Register your center",
  description: "Create your MotorsWinding.com center account.",
};

export default function RegisterLayout({ children }) {
  return children;
}
